{
  "name": "@uniblox/shared",
  "private": true,
  "version": "1.0.0",
  "description": "Shared Zod schemas and types — single source of truth for API contracts",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests in shared (schemas are exercised via api tests)\""
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
